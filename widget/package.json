{
  "name": "firstvoid-widget",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/widget.ts --bundle --minify --format=iife --target=es2018 --outfile=dist/widget.js",
    "dev": "esbuild src/widget.ts --bundle --format=iife --target=es2018 --outfile=dist/widget.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
